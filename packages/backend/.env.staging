# Backend staging environment
PORT=3001
BACKEND_URL=https://xpogo-backend.vercel.app
NODE_ENV=staging

# PostgreSQL Database (staging)
DB_USER=staging_user
DB_PASSWORD=staging_password
DB_HOST=staging-db.example.com
DB_PORT=5432
DB_NAME=xpogo_staging

# JWT Secret (change to secure value in actual staging!)
JWT_SECRET=staging-secret-key-change-in-prod
JWT_EXPIRY=7d
